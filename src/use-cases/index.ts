export * as authFeatures from "./auth" // completeRegistration — registers Firebase Auth user + creates Firestore profile
export * as workspaceFeatures from "./workspace" // createWorkspaceWithCapabilities — creates workspace + mounts initial capabilities
export * as accountFeatures from "./account" // setupOrganizationWithTeam — creates org + provisions initial team
